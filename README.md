# tgbot-stock - Telegram 股票持仓 Bot

群友股票买卖记录、持仓查看、损益计算，接入实时行情。

## 安装

```bash
pip install -r requirements.txt
```

## 配置

设置环境变量：

```powershell
$env:TG_BOT_TOKEN = "你的Bot Token"
python main.py
```

或 Linux/Mac：

```bash
export TG_BOT_TOKEN="你的Bot Token"
python main.py
```

## 命令

| 命令 | 说明 | 示例 |
|------|------|------|
| `/quote SYMBOL` | 查询实时行情 | `/quote 600000` |
| `/buy SYMBOL PRICE QTY [Nx]` | 买入或按指定杠杆回补空仓 | `/buy 600000 10.50 100 5x` |
| `/sell SYMBOL PRICE QTY [Nx]` | 卖出或按指定杠杆建立空仓 | `/sell 600000 11.00 100 5x` |
| `/buya SYMBOL PRICE AMOUNT [Nx] [Ns]` | 按仓位金额计算不超预算的最大买入量 | `/buya 600000 10.50 10000 5x 100s` |
| `/sella SYMBOL PRICE AMOUNT [Nx] [Ns]` | 按仓位金额计算不超预算的最大卖出量 | `/sella 600000 11.00 10000 5x 100s` |
| `/buy SYMBOL PRICE ALL [Nx]` | 平全部空头，或只平指定杠杆空头 | `/buy 600000 10.50 ALL`；`/buy 600000 10.50 ALL 5x` |
| `/sell SYMBOL PRICE ALL [Nx]` | 平全部多头，或只平指定杠杆多头 | `/sell 600000 11.00 ALL`；`/sell 600000 11.00 ALL 5x` |
| `/close SYMBOL PRICE` | 一次平掉该代码的全部多空杠杆条目 | `/close 600000 11.00` |
| `/position` | 查看持仓和浮盈 | `/position` |
| `/pnl` | 查看损益汇总 | `/pnl` |
| `/lb` | 收益率排行榜 | `/lb` |
| `/trades` | 查看交易记录（含ID） | `/trades` |
| `/del [ID]` | 删除交易记录 | `/del` 或 `/del 3` |
| `/update` | 将旧数据库一次规整为当前 v1 | `/update` |

## SYMBOL 格式（含基金）

| 市场 | 格式 | 示例 |
|------|------|------|
| A股（沪） | 6位数字，5/6/9开头 | 600000 510050 |
| A股（深） | 6位数字，0/1/2/3开头 | 000001 159919 |
| 港股 | 5位数字 | 00700 02800 |
| 美股 | 字母 | AAPL TSLA |
| 场外基金 | 6位数字（自动识别） | 010042 |

## 行情来源

- A股/港股：新浪财经（免费，延迟~3秒）
- 美股：Yahoo Finance
- 场外基金：天天基金（估算净值）
- 行情按“证券代码 + 市场”分别缓存 3 秒，避免同代码股票与基金串线
- 买入/卖出价格偏离现价 >5% 时需二次确认，按钮仅有发起人本人可操作
- 支持负持仓（先卖后买）；空头价格上涨时亏损、下跌时盈利
- QTY 使用“股”为输入单位并支持最多两位小数；例如输入 `1` 股存为整数 `100`，输入 `0.01` 股存为整数 `1`
- `/buya` 和 `/sella` 用 `AMOUNT / PRICE` 算量并按最小单位向下取整，保证成交额不超过 `AMOUNT`；最小单位 `100s`、`1s`、`01s`、`001s` 分别代表 100、1、0.1、0.01 股，省略时为 0.01 股，连一份最小单位也买不起时不会进入确认流程
- 杠杆是交易命令的第 4 个参数，只接受以 `x` 结尾的格式（如 `1x`、`2.5x`、`5x`）；首次建仓可省略并按 `1x` 处理，已有持仓后的普通 B/S 必须显式填写
- 同一代码按市场和杠杆分成独立持仓条目；同方向使用新杠杆时会新建条目并在确认消息中提醒
- 普通反向交易必须填写目标条目的正确杠杆，包括平 `1x` 仓也必须写 `1x`；缺少或写错时操作无效
- `SELL ... ALL` 一次平掉全部多头条目，`SELL ... ALL 5x` 只平 `5x` 多头；`BUY ... ALL [Nx]` 对空头使用相同规则
- `/close SYMBOL PRICE` 在一个数据库事务中一次关闭该代码的全部多空杠杆条目
- 港股价格及名称按新浪港股字段解析，交易金额以 HKD 记录并按实时汇率折算 CNY
- `/update` 期间机器人进入维护模式：开始时提示暂停操作，交易、查询和确认按钮会被拦截，事务结束后再通知恢复使用

## 数据结构

所有交易记录存储在 `data/trades.db`，市场会在确认交易时与证券代码一起入库，持仓按“证券代码 + 市场 + 杠杆”聚合为独立条目，并分别计算多空方向的加权平均开仓价。数量以 `0.01` 股为最小整数单位存储。旧数据库升级后执行一次 `/update`：唯一的 v0→v1 迁移会同时把历史数量乘以 `100`、增加杠杆与市场字段，并把历史交易设为 `1x`、按代码补齐市场。版本号与数据在同一事务提交，重复执行不会再次转换。升级前的短暂窗口仍能兼容读取旧数量格式。

## 代码结构

- `handlers/`：Bot 路由包；`__init__.py` 负责组合路由，命令实现按 `system.py`、`trading.py`、`callbacks.py`、`reports.py` 和 `common.py` 分层
- `models/`：数据访问包；`__init__.py` 暴露稳定接口，领域类型、数据库迁移和交易仓储分别位于 `types.py`、`schema.py` 和 `repository.py`
- `pnl.py`：持仓成本、盈亏与排行榜计算
- `quotes.py`：证券代码识别、行情和汇率获取
